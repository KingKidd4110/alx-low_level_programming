#include "lists.h"

/**
 * dlistint_len - returns number of elements
 * @h: el header
 *
 * Return: Number of el
 */

size_t dlistint_len(const dlistint_t *h)
{
	int i;

	for (i = 0, head != NULL, i++)
		i++;
	return (i);
}



